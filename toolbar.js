export default class toolbar {
    constructor(location = 'top',
                width = '20px',
                buttons = ['hello, toolbar!'],
                stretch = true) {
        console.log(location);
    }
}