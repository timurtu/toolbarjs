export default function toolbar(location = 'top',
                                width = '20px',
                                buttons = ['hello, toolbar!'],
                                stretch = true) {
    console.log(location);
}
